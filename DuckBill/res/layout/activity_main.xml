<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".MainActivity" >

    <ListView
        android:id="@+id/songs"
        android:layout_width="match_parent"
        android:layout_height="fill_parent"
        android:layout_above="@+id/mergeButton"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:animateLayoutChanges="true"
        android:background="#2AA7F5"
        android:fadeScrollbars="false"
        android:scrollbarStyle="outsideOverlay" >
    </ListView>

    <Button
        android:id="@+id/musicButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignRight="@+id/songs"
        android:layout_marginBottom="40dp"
        android:text="Add Music" />

    <Button
        android:id="@+id/removeAudio"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@+id/musicButton"
        android:layout_alignBottom="@+id/musicButton"
        android:layout_centerHorizontal="true"
        android:text="Remove Audio" />

    <Button
        android:id="@+id/mergeButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@+id/removeAudio"
        android:layout_alignBottom="@+id/removeAudio"
        android:layout_alignLeft="@+id/songs"
        android:text="Merge Videos" />

</RelativeLayout>
